# Loaded by Vite ONLY in dev mode (`npm run dev`) — never in production builds.
# Enables the no-Discord dev-login UI (persona switcher + dev login buttons) so
# contributors can sign in locally without a Discord OAuth app.
VITE_DEV_PERSONAS=true
